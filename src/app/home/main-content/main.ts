export interface Product {
  image: string,
  name: string,
  inventoryStatus: string

  price: number


}
export interface ResponsiveOptions {
  breakpoint: string,
  numVisible: number,
  numScroll: number
}